// This worker is no longer used.
// Biome finding is now performed in the main thread via useBiomeFinderStore,
// using the already-initialized biomeSource and sampler from useLoadedDimensionStore.
export {}
